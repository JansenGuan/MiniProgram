
<view class='container'>
  <block wx:for="{{titles}}" wx:key="{{index}}">
    <view class='subTab' id='eventRes' bindtap='selectedTab' data-index='{{index}}'>
      <text class='text-tab {{currentIndex == index ? "active" : ""}}'>{{item}}</text>
    </view>
  </block>
</view>